# Multiplex SSH to coder dev boxes — first connect opens a master socket,
# subsequent ssh/rsync calls within ControlPersist reuse it.
Host sfeng-dev.coder
    ControlMaster auto
    ControlPath ~/.ssh/cm-%C
    ControlPersist 10m
